name: Deploy Documentation

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install all dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs-material mkdocs-section-index mkdocs-glightbox mkdocs-minify-plugin mkdocs-awesome-pages-plugin

      - name: Build Public Site
        run: mkdocs build -f mkdocs.yml

      - name: Build Internal Site
        run: |
          if [ -f "mkdocs-internal.yml" ]; then
            mkdocs build -f mkdocs-internal.yml
          else
            echo "Internal config not found, skipping."
          fi